# MCP server dependencies

# MCP SDK (stdio transport, tool/server primitives)
mcp>=1.0.0

# GDB/MI client — communicate with VirtualBox's built-in GDB stub
pygdbmi>=0.11.0

# Test dependencies
pytest>=8.0.0
pytest-asyncio>=0.24.0
